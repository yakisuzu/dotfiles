# Local Settings (personal, not committed)

## Plan Group

Map working directory patterns to plan group names.
Plans are saved to `~/.claude/plans/<group>/`.

Format: `- <dir_pattern>: <group>`

Mappings:

